<li>
    <a class="inline-flex items-center gap-1 text-sm transition-all hover:translate-x-2" href="{{ $bookmark->url }}">
        {{ $bookmark->svgIcon('h-3 w-3') }}
        {{ $bookmark->name }}
    </a>
</li>
